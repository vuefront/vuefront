query($search: String){
    searchProduct: productsList(page: 1, size: 12, sort: "date_added", order: "DESC", search: $search) {
        content {
            id
            name
            shortDescription
            price
            special
            image
            imageLazy
            rating
        }
    }
}
