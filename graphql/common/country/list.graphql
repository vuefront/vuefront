query($page: Int, $size: Int) {
  countriesList(page: $page, size: $size) {
    content {
      id
      name
    }
    totalPages
    totalElements
    first
    last
    number
    numberOfElements
  }
}
