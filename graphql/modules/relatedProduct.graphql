query($id: String) {
    relatedProducts: product(id: $id) {
        id
        products(limit: 4) {
            id
            image
            imageLazy
            description
            shortDescription
            price
            special
            name
            keyword
        }
    }
}
